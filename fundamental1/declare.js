// here we declare variable in different ways
// let, var, const

// we use let when we want to mutate the value. we declare the variable 
// use of let but we want to give value to it later.\
// this is mutable
let age= 20;
age= 33;
console.log(age);


// the value cannot be changed for example-:
const  birthYear=1909;

// in this the variable cannot be muted
// this is immutable
// birthYear=1990;

// old way to declaring the values
// this is similar to let

var job="teacher"; 
job="coder";


// we can declare value without using keyword
// but we have to use keywords before declaring the variable fot good practice
lastname="BOB";
console.log(lastname);