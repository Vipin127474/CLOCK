// javascript versions

// ES1 code

function add(n)
{
    var x=5+ add.arguments[0];
    return x;
}

// this code is also run in modern version of javascript. this means
// that there is almost never anything removed only added new versions.

// there is only backward compatibility not forward compatibility
// menas that the old version code can run in modern javascript but 
// but the future code does not run in today's browser